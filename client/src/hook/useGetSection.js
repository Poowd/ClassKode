import axios from "axios";
import { useState } from "react";

export default function useGetSection(x, y, z) {
  const ProgramList = [x];
  const YearList = [y];
  const SemList = [z];



  function getSection(program, year, sem) {
    



  }

  return [getSection];
}
